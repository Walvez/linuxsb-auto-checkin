# linux.sb 自动签到（脚本猫 ScriptCat 定时脚本）

> 无需复制 Cookie，也无需保持 linux.sb 网页打开。登录一次，之后由 ScriptCat 在浏览器后台每天自动完成签到。

## ✨ 功能亮点

- 每天在 ScriptCat 后台自动签到
- **打开浏览器 5 分钟内自动签到**（定时为每 5 分钟触发一次，`once` 保证当天只真正执行一次）
- 自动使用当前浏览器中的 linux.sb 登录状态
- 准确区分签到成功、今日已签到、登录失效和请求异常
- 失败后在当天的下一个候选时刻自动重试
- 脚本菜单支持立即签到和打开签到页
- 只连接 linux.sb / www.linux.bi，不包含统计、广告、返利链接或第三方代码

## 🚀 三步开始

### 1. 安装扩展

安装并启用 [脚本猫 ScriptCat](https://scriptcat.org/)。

### 2. 登录 linux.sb

在同一个浏览器中打开 [linux.sb](https://linux.sb/) 并完成登录。

### 3. 启用脚本

在脚本猫中安装并启用本脚本，首次使用建议从脚本菜单执行一次「立即签到」。

收到「linux.sb 签到成功」或「linux.sb 今日已签到」通知，就表示配置完成。之后不需要保持 linux.sb 网页打开。

## 🔔 通知说明

| 通知标题 | 含义 |
| :--- | :--- |
| **linux.sb 签到成功** | 本次签到成功 |
| **linux.sb 今日已签到** | 今天已经完成签到 |
| **linux.sb 需要重新登录** | 登录状态已失效，重新登录后再次签到即可（点击通知可打开签到页） |
| **linux.sb 签到失败** | 网络或网站接口异常，会在下一个候选时刻自动再试 |

## 签到原理

linux.sb 的每日签到走 `/daily_checkin`：

1. `GET /daily_checkin` —— 读取状态（`今天待签到 / 今日已签到 / 请先登录` 等）并提取表单 `_csrf` 令牌
2. 若未签到，`POST /daily_checkin`，body 为 `_csrf=<token>`
3. 已签到则跳过，不再重复请求（并本地记录当日状态）

脚本编写的接口逻辑参考了开源项目 [vfhky/linux-sb-pro](https://github.com/vfhky/linux-sb-pro)（Apache-2.0）。

## ⚙️ 调度说明

- 元数据使用 `@crontab 5-55/5 * once * *`：每 5 分钟触发一次，`once` 保证一个周期内只执行一次。
- 因此**只要浏览器开着，打开浏览器后的 5 分钟内就会自动完成签到**；无人时（浏览器关闭）定时不会执行。
- 可自行修改脚本顶部的 `@crontab` 调整触发频率。

## 🧭 脚本菜单

安装后可在脚本猫脚本列表中找到本脚本的菜单项：

- **立即签到**：手动触发一次签到并给出结果
- **打开签到页**：在新标签打开 linux.sb 的签到页

## 免责声明

仅供学习交流。请遵守 linux.sb 站点规则，合理使用自动签到功能。
